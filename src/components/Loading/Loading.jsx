import React from 'react';
import './Loading.css';

const Loading = () => {
    return (
        <div className="Loading-container">
            loading...
        </div>
    );
};

export default Loading;